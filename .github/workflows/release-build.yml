name: Build the release
run-name: ${{ github.actor }} is building on ${{github.ref_name}} 🚀
on:
  push:
    tags:
      - rc-0.0.*
jobs:
  Test-and-lint-code:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/tests-on-push
